# MỤC TIÊU
Khám phá SÂU module được cung cấp bằng cách tương tác thật trên browser, đi hết mọi
thao tác/luồng (kể cả luồng đa trang), rồi sinh tài liệu Requirements CHUẨN BA
(developer-ready & tester-ready), tổ chức theo LUỒNG THAO TÁC.

# KẾT HỢP 2 SKILL (Pipeline — theo đúng thứ tự)
1) /explore-website-requirements-by-flow  → ĐỘNG CƠ CHÍNH.
   - Crawl KHÔNG GIỚI HẠN THAO TÁC: đi hết element/menu/tab/modal/nhánh rẽ.
   - Chạy PLAYBOOK theo archetype UI (bảng CRUD, form/modal, wizard, master-detail,
     search/filter/sort/pagination, phân quyền) để không bỏ sót thao tác.
   - Theo dấu LUỒNG end-to-end, vẽ sơ đồ Mermaid, lập Ma trận Coverage + RTM.
   - Sinh requirements chuẩn BA: mã hóa FR/BR/NFR/UC/FLOW, User Story INVEST,
     Acceptance Criteria (Gherkin), MoSCoW, Data Dictionary, Use Case Spec.
2) /generate-requirements-from-website (kèm /requirements-analyzer) → CHUẨN ĐỊNH DẠNG.
   - Nạp để lấy chuẩn trình bày SRS (Overview, Functional Requirements,
     Field Specifications, Business Rules & Validations).
   - Dùng như bộ khung/định dạng tham chiếu cho tài liệu cuối; KHÔNG thay thế bước
     khám phá tương tác ở (1).
   - Khi có mâu thuẫn định dạng, ưu tiên cấu trúc đầu ra của skill (1) vì đã bao trùm
     và chi tiết hơn (theo luồng + RTM + Gherkin).

# CONTEXT  (thay các giá trị dưới đây cho module thực tế của bạn)
- Mô tả hệ thống: [Mô tả ngắn gọn về hệ thống hiện tại, module Edit menu]
- Website URL: https://order-flutter.nasys.vn/pos-shell-route/menu-edit-list-route
- Module Name: menu
  # Lưu ý: combo này phát huy tối đa với MODULE NGHIỆP VỤ có nhiều thao tác/luồng
  # (VD: Edit menu, Điều phối ca, Return Order, Order...). Với trang đơn giản như Login,
  # Playbook archetype và cảnh báo thao tác phá hủy gần như không áp dụng — vẫn chạy
  # được nhưng chỉ khai thác được phần nhỏ năng lực của skill.
  # Gợi ý thao tác/luồng thường có ở Edit menu (kiểm chứng trên UI thật, KHÔNG bịa):
  #   - Danh sách món ăn + các cột (Mã hàng hóa, Đơn vị tính, Trạng thái...).
  #   - Bật/tắt trạng thái món (đang bán ↔ ngừng bán) ở cột Trạng thái.
  #   - Tạo mới món (modal): Mã hàng hóa (tự động theo quy tắc / nhập tay), tên, giá, ảnh.
  #   - Tải/thêm ảnh cho món; xem ảnh trong modal chi tiết món.
  #   - Sửa/xóa món; tìm kiếm / lọc / sắp xếp / phân trang danh sách món.
  #   - Ràng buộc: mã món trùng, trường bắt buộc, định dạng/kích thước ảnh.
  #   - Phân quyền (tài khoản Thu ngân KHÔNG được vào/thao tác Edit menu).
- Đường đi tới module (cách BẬT/mở module menu):
  #   1. Đăng nhập vào hệ thống (Cashier).
  #   2. Nhấn tổ hợp phím Ctrl + F9 → hiện lên menu (thanh/lưới icon).
  #   3. Click và GIỮ 2 giây vào icon "Menu" để vào trang Edit menu.
  #   (Không phải click thường — phải nhấn giữ đủ ~2s mới mở được trang.)
- Thông tin đăng nhập: thientester/admin/12345678  (tài khoản TEST, được phép thao tác)
- Scope loại trừ (không đụng tới): [nếu có]

# TIỀN ĐIỀU KIỆN AN TOÀN (BẮT BUỘC)
- CHỈ chạy trên môi trường TEST/STAGING bằng tài khoản TEST ở trên.
- Skill có thể tạo/sửa/xóa dữ liệu thật khi khám phá → xác nhận 1 lần với user trước khi
  bắt đầu, và ghi lại mọi thao tác phá hủy vào Ma trận Coverage.
- Cần Playwright/Browser MCP để tương tác DOM thật. Nếu CHƯA kết nối browser MCP:
  DỪNG và báo user — KHÔNG đoán locator / không suy diễn field.

# YÊU CẦU THỰC THI
- Truy cập module theo đúng "Đường đi tới module" ở trên: đăng nhập → Ctrl+F9 →
  click GIỮ 2s vào icon "Menu". Thao tác giữ 2s thực hiện bằng mouse-down → chờ ~2s →
  mouse-up (KHÔNG dùng click thường; KHÔNG hard-sleep cho phần chờ điều kiện khác).
  Playwright MCP: dùng bàn phím gửi Ctrl+F9, rồi press-and-hold trên icon Menu.
  Nếu chưa vào được trang → đọc lại DOM (browser_snapshot), thử lại; vẫn không được → báo user.
- Set viewport desktop 1920x1080, dùng browser_snapshot làm nguồn chân lý về DOM.
- KHÔNG bịa field/nút/validation không nhìn thấy. Logic không quan sát được → đưa vào
  mục "Câu hỏi làm rõ với PO/User".
- Mọi FR/BR/NFR/UC có MÃ DUY NHẤT; mỗi luồng có User Story + Acceptance Criteria (Gherkin)
  + độ ưu tiên MoSCoW; toàn tài liệu phải có Ma trận Coverage và RTM (truy vết về bằng chứng).

# OUTPUT FORMAT
- Toàn bộ tài liệu bằng Tiếng Việt, chuyên nghiệp.
- Xuất file Markdown: requirements/requirements_edit_menu.md
  (theo cấu trúc SRS 10 mục của skill /explore-website-requirements-by-flow).
- Nếu tài liệu dài, trình bày qua Artifact để tiện lưu/xuất file.
